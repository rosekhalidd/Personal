/* ============================================================
   Cloud sync config.
   Paste your two Supabase values between the quotes below, then
   save. Leave them empty to keep the app local-only (no sync).
   Both values are safe to publish — the database is protected by
   row-level security, so only your signed-in account sees your data.
   ============================================================ */
window.HEALTH_CONFIG = {
  SUPABASE_URL: '',       // e.g. https://abcdefgh.supabase.co
  SUPABASE_ANON_KEY: '',  // the long "anon public" key
};
